{
  "low": 0,
  "moderate": 2,
  "high": 8,
  "critical": 0,
  "_note": "Production-audit floor for accepted UPSTREAM debt. What CAN be fixed has been: js-yaml was pinned to ^4.3.2 / ^3.15.2 via overrides (that is why this floor is 8 high, not 10). The remainder is transitive through @bagsfm/bags-sdk -> @meteora-ag/* / @coral-xyz/anchor / @solana/* and has no fix that does not break the SDK stack (npm's own `fixAvailable` for bigint-buffer/spl-token/buffer-layout-utils resolves by pulling @bagsfm/bags-sdk down to 1.0.8 and web3.js to 0.0.3 — a build with no trade/partner services). Most of it is one root cause, bigint-buffer GHSA-3gc7-fjrx-p6mg, reported at every level of that chain; 1.1.5 is both installed and latest and the advisory covers all published versions. toml, jayson and stream-json entered the tree the same way. npm honours `overrides` only in the ROOT project, so those protect this repo/CI but NOT consumers of the published package; closing these for consumers needs the SDK to unpin its own transitives upstream. critical>0 still fails hard; any rise above these counts still fails. Lower this file whenever a further fix lands or upstream ships one.",
  "_recorded": "2026-09-24"
}
